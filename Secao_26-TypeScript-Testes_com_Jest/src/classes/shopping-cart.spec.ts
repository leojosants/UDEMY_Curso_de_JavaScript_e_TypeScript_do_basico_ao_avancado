import { ShoppingCart } from './shopping-cart';

describe('ShoppingCart', () => {
  it('should be an empty cart when no product is add', () => {
    class DiscoutMock
    const sut = new ShoppingCart();
  });
});
